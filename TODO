- SMTP SSL and/or TLS
- auth on http proxying
- http (hotmail) fetching
- look into GSSAPI, POP3's MD5 thing, all that other crap
- filter based on attachment size, quantity, type?
- redo chrooting properly (resolver needs to be in parent)
- fetch from mbox
- test/fix multiuser (X-Envelope-To, weed out duplicate users)
- finish regression tests
- message-id cache, weed out message duplicates
- balance man pages/manual
- match:
	save list of matching attachments in mail

	match attachment count <> n
	match attachment total-size <> n
	match attachment any-size <> n
	match attachment any-type "fnmatch"
	match attachment any-name "fnmatch"
	match attachment all
	match attachment

	expressions:

	match attachment (index <> n and size < 200 and type "fnmatch")

must all be in child:
	action strip-attachments
	action strip-attachments
	action pipe-attachments "cmd" 

- move decision into mctx/dctx
- merge dctx/mctx?
